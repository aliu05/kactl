#include <bits/stdc++.h>
using namespace std;

typedef int64_t i64;

#define nl '\n'
#define pb push_back
#define mp make_pair
#define YES "YES\n"
#define NO "NO\n"
#define F0R(a, b) for(i64 (a)=0; (a)<(b); ++(a))
#define FORE(a) for(auto& e : (a))
#define all(x) (x).begin(), (x).end()
#define sz(x) (i64)(x).size()

// Lots of the book code uses this one
#define rep(i, a, b) for(int i = a; i < (b); ++i)

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}